# --- Core web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Database ---
sqlalchemy==2.0.35
pymysql==1.1.1          # MySQL DB-API driver used by SQLAlchemy
cryptography==43.0.1    # required by pymysql for auth plugins on modern MySQL

# --- Dev/test (used from V1 onward so tests can be added anytime) ---
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.16.1
httpx==0.27.2           # also used directly as the async client for Gemini/Telegram/WhatsApp

# --- File upload support (needed for the /meals/upload endpoint) ---
python-multipart==0.0.12

# --- Scheduler ---
apscheduler==3.10.4

# --- Vector search / retrieval (V3, LangChain-based) ---
langchain==0.3.7
langchain-community==0.3.7
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2
faiss-cpu==1.15.0             # backing FAISS implementation LangChain's vectorstore wraps
sentence-transformers==3.2.1 # backing model runner HuggingFaceEmbeddings wraps
